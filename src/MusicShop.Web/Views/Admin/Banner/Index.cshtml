@model IEnumerable<MusicShop.Data.Entities.Banner>
@{
    ViewData["Title"] = "幻燈片管理";
    ViewData["PageId"] = "admin-banners";
}

<partial name="_TempDataNotifications" />

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>🖼️ 幻燈片管理</h3>
        <a asp-action="BannerCreate" class="btn btn-primary">➕ 新增幻燈片</a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>順序</th>
                    <th>預覽</th>
                    <th>連結商品</th>
                    <th>狀態</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var banner in Model)
                {
                    <tr>
                        <td>@banner.DisplayOrder</td>
                        <td>
                            @if (!string.IsNullOrEmpty(banner.ImageUrl))
                            {
                                <img src="@banner.ImageUrl" alt="幻燈片" style="height:60px;object-fit:cover;border-radius:4px;">
                            }
                            else
                            {
                                <span class="text-muted">尚未上傳圖片</span>
                            }
                        </td>
                        <td>
                            @if (banner.Album != null)
                            {
                                <a asp-controller="Album" asp-action="Detail" asp-route-id="@banner.AlbumId" target="_blank">
                                    @banner.Album.Title
                                </a>
                            }
                            else
                            {
                                <span class="text-muted">純展示（無連結）</span>
                            }
                        </td>
                        <td>
                            @if (banner.IsActive)
                            {
                                <span class="badge bg-success">顯示中</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">已停用</span>
                            }
                        </td>
                        <td>
                            <a asp-action="BannerEdit" asp-route-id="@banner.Id" class="btn btn-sm btn-outline-primary">編輯</a>

                            <form asp-action="BannerToggle" method="post" class="d-inline">
                                <input type="hidden" name="id" value="@banner.Id" />
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm @(banner.IsActive ? "btn-outline-warning" : "btn-outline-success")">
                                    @(banner.IsActive ? "停用" : "啟用")
                                </button>
                            </form>

                            <form asp-action="BannerDelete" method="post" class="d-inline"
                                  onsubmit="return confirm('確定要刪除這張幻燈片嗎？')">
                                <input type="hidden" name="id" value="@banner.Id" />
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger">刪除</button>
                            </form>
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted py-4">尚未新增任何幻燈片</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
